<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="style.css">
    <style>
        body {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
    background-color: #f0f0f0; /* Set a background color, change as needed */
}

.main {
    text-align: center;
}

form {
    width: 300px; /* Adjust the width of the form as needed */
    padding: 20px;
    border: 1px solid #3498db; /* Blue border for the form, change as needed */
    background-color: #fff; /* White background for the form, change as needed */
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Optional box shadow for a subtle effect */
}

textarea {
    width: calc(100% - 20px);
    padding: 10px;
    margin: 5px 0;
    box-sizing: border-box;
    border: 1px solid #3498db; /* Blue border for textareas, change as needed */
}

button {
    width: 100%;
    padding: 10px;
    background-color: #3498db; /* Blue color for the button, change as needed */
    color: #fff; /* White text, change as needed */
    border: none;
    cursor: pointer;
    font-size: 16px;
}

button:hover {
    background-color: #2980b9; /* Darker shade of blue on hover, change as needed */
}

/* Add more styling as needed */

    </style>
    
</head>
<body>
    <div class="main">
        <form method="POST" action="/<%=data._id%>?_method=PUT">
            <textarea placeholder="row1" name="r1"><%= data.r1  %></textarea> 
            <textarea placeholder="row2" name="r2"><%= data.r2  %></textarea> 
            <button>Save changes</button>
        </form>
    </div>
</body>
</html>